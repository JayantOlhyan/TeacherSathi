import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact Support",
  description: "Get in touch with the TeacherSathi team. Reach out for classroom onboarding assistance, technical queries, institutional partnerships, or educator support.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
